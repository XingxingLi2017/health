<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xing.dao.AccessKeysDao">
    <select id="getMailKey" resultType="map">
        select * from t_email_sender
        where senderId = 1
    </select>
</mapper>